"""price-watcher — a hub for per-site price/availability watchers.

The harness (registry, history, rules, notification) is written once and shared.
Each site contributes one small adapter under `pricewatch.adapters`.
"""

__version__ = "0.1.0"
